<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">

    
</head>
<body>
    <div class="register-form">
        <h1>Registration Form</h1>
        <form action="/update"  method= "POST">

            <input type="number" name= "id" hidden >

            <label for="user">User</label>
            <input type="text" name= "user" value="<%= results.user%>" required>

            <label for="name">Full Name</label>
            <input type="text" name= "name" value="<%= results.name%>" required>

            <select name="rol" id="rol" class="select-css">
                <option <%= results.rol == 'admin' ? 'selected' : '' %> value="admin" >Admin</option>
                <option <%= results.rol == 'dataentry' ? 'selected' : '' %> value="dataentry">Data Entry</option>
                <option <%= results.rol == 'superusuario' ? 'selected' : ''  %> value="superusuario">Super Usuario</option>
            </select>

            <label for="pass">Password</label>
            <input type="password" name="pass" >


        <div style="display: inline;" >

            <a href="/" class="btn-cancel">cancel</a><input type="submit" class="btn-save" value="Save">
            

        </div>
        </form>

    </div>
    
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>

      <% if(typeof alert != "undefined") { %>
        
        <script>
            Swal.fire({
                title: '<%= alertTitle %>' ,
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton: '<%= showConfirmButton %>',
                timer: '<%= timer %>'
            }).then(()=>{
                window.location= '/<%= ruta %>'
            })

        </script>   
          

          <% }  %>

       


   
</body>
</html>
